properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage("Build image") {
            steps {
                echo "============================== start ================================="
                dir ('jenkins') {
                    sh 'docker build -t cdlab Dockerfile'
                }
            }
        }
    }
}
