properties([disableConcurrentBuilds()])
pipeline {
    agent {
label 'master'
    }

   
    stages {
        stage('Build Docker Image') {
            steps {
echo "============================== start ============================="
                dir ('.') {
sh 'docker build .'
            }
        }
    }
}
