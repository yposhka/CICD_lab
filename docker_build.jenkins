properties([disableConcurrentBuilds()])

pipeline {
    agent any
    stages {
        stage("Build image") {
            steps {
                echo "============================== start ================================="
                sh 'docker build -t my-first-build .'
            }
        }
    }
}
