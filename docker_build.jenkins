properties([disableConcurrentBuilds()])

pipeline {
    agent {
        label 'master'
    }
    stages {
        stage("Build image") {
            steps {
                echo "============================== start ================================="
                dir ('./') {
                    sh 'docker build .'
                }
            }
        }
    }
}
